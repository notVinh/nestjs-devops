export enum TOKEN_STATUS {
  Active = 1,
  InActive = 0,
}

export enum NOTIFICATION_STATUS {
  Seen = 1,
  UnSeen = 0,
}

// Deprecated: Use TOKEN_STATUS and NOTIFICATION_STATUS instead
export enum STATUS_NOTIFICATION {
  Active = 1,
  InActive = 0,
  Seen = 1,
  UnSeen = 0,
}
