global:
  scrape_interval: 15s # Cứ 15 giây lấy dữ liệu một lần

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Lấy thông số máy ảo Ubuntu